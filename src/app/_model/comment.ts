// import { Person } from './person';
// import { Like } from './like';

// import { Person } from './person';

export interface Comment {
  id?: number;
  body?: string;
  postId?: number;
  user?: number;
  likeId?: number;
}
